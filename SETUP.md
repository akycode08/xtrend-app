# 🚀 Инструкция по настройке TrendScout-V2 с нуля

## Предварительные требования

- Python 3.9+ 
- Node.js 18+ и npm
- PostgreSQL с поддержкой расширения `pgvector` (или Supabase)
- API ключи: Apify, Anthropic

---

## Шаг 1: Настройка Backend (Python/FastAPI)

### 1.1 Перейдите в папку backend
```bash
cd backend
```

### 1.2 Создайте виртуальное окружение (если еще не создано)
```bash
python3 -m venv venv
```

### 1.3 Активируйте виртуальное окружение

**На macOS/Linux:**
```bash
source venv/bin/activate
```

**На Windows:**
```bash
venv\Scripts\activate
```

### 1.4 Установите зависимости
```bash
pip install -r requirements.txt
```

### 1.5 Настройте переменные окружения

Убедитесь, что файл `.env` находится в папке `backend/` и содержит:

```env
# База данных
DATABASE_URL=postgresql://user:password@host:port/database

# API ключи
APIFY_API_TOKEN=ваш_apify_token
ANTHROPIC_API_KEY=ваш_anthropic_key
apify_token=ваш_apify_token

# Настройки безопасности
SECRET_KEY=ваш_секретный_ключ
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Neo4j (если используется)
NEO4J_URI=neo4j+s://ваш_uri
NEO4J_USER=neo4j
NEO4J_PASSWORD=ваш_пароль
```

### 1.6 Запустите backend сервер
```bash
# Из папки backend/
python -m app.main

# Или через uvicorn напрямую
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

Сервер запустится на: **http://localhost:8000**

---

## Шаг 2: Настройка Frontend (Next.js/React)

### 2.1 Перейдите в папку frontend
```bash
cd frontend
```

### 2.2 Установите зависимости
```bash
npm install
```

### 2.3 Запустите dev сервер
```bash
npm run dev
```

Frontend запустится на: **http://localhost:3000**

---

## Шаг 3: Проверка работы

### 3.1 Проверьте backend
Откройте в браузере: http://localhost:8000

Должны увидеть:
```json
{
  "status": "ok",
  "version": "2.0.0",
  "engine": "6-layer-math-v2"
}
```

### 3.2 Проверьте API документацию
Откройте: http://localhost:8000/docs (Swagger UI)

### 3.3 Проверьте frontend
Откройте: http://localhost:3000

---

## Шаг 4: Настройка базы данных

Проект автоматически создаст таблицы при первом запуске (см. `app/main.py`).

Если нужно вручную:

```python
from app.core.database import Base, engine
Base.metadata.create_all(bind=engine)
```

**Важно:** Убедитесь, что в PostgreSQL установлено расширение `pgvector`:
```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

---

## Частые проблемы

### Проблема: "Module not found"
**Решение:** Убедитесь, что виртуальное окружение активировано и зависимости установлены

### Проблема: "Connection refused" (база данных)
**Решение:** Проверьте `DATABASE_URL` в `.env` файле и убедитесь, что PostgreSQL запущен

### Проблема: "pgvector extension not found"
**Решение:** Установите расширение в PostgreSQL или используйте Supabase (он поддерживает pgvector)

### Проблема: CORS ошибки
**Решение:** Проверьте настройки CORS в `app/main.py` - по умолчанию разрешены все origins

---

## Структура проекта

```
TrendScout-V2/
├── backend/
│   ├── app/
│   │   ├── api/          # API endpoints
│   │   ├── core/         # Конфигурация и база данных
│   │   ├── db/           # Модели базы данных
│   │   ├── services/     # Бизнес-логика
│   │   └── main.py       # Точка входа
│   ├── requirements.txt
│   └── .env              # Переменные окружения
│
└── frontend/
    ├── src/
    │   ├── app/          # Next.js pages
    │   ├── components/   # React компоненты
    │   ├── lib/          # Утилиты и API клиент
    │   └── types/        # TypeScript типы
    ├── package.json
    └── .env              # Frontend переменные (если нужны)
```

---

## Готово! 🎉

После выполнения всех шагов:
- ✅ Backend работает на http://localhost:8000
- ✅ Frontend работает на http://localhost:3000
- ✅ База данных подключена
- ✅ API доступен на /api/trends, /api/profiles и т.д.
